version: '3'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    image: docker_image
    container_name: docker-container
    ports:
      - "8000:8000"
    volumes:
      - .:/app


#required for local development

#   db:
#     image: postgres:17
#     container_name: postgres-db
#     restart: always
#     environment:
#       POSTGRES_USER: my_user
#       POSTGRES_PASSWORD: my_password
#       POSTGRES_DB: my_database
#     volumes:
#       - postgres_data:/var/lib/prosgresql/data
#     ports:
#       - "5432:5432"
# volumes:
#   postgres_data: